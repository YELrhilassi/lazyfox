{
  "name": "lazyfox",
  "version": "0.5.2",
  "private": true,
  "description": "Keyboard-first, UI-free Firefox. Leader-key navigation, paginated which-key overlay, centered search, tab switcher and link hints.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev",
    "build:installer": "go build -trimpath -ldflags=\"-s -w\" -o installer/bin/lazyfox-install ./installer",
    "typecheck": "tsc --noEmit",
    "test": "go test ./core/ && go test ./installer/ && node scripts/check-dist.mjs",
    "test:installer": "go test ./installer/",
    "prepare": "node scripts/check-toolchain.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
